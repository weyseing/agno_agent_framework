# Setup Guide
- **Copy `.env.example` to `.env` & fill up below**
```properties
GOOGLE_API_KEY=
```

- **Install node dependencies**
```bash
cd frontend
npm install
```

- **Setup Docker containers**
```bash
docker compose up -d
```